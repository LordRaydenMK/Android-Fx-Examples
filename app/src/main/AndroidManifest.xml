<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.github.lordraydenmk.android_fx">

    <application
        android:name=".AndroidFxApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".example06.RetryingActivity"></activity>
        <activity android:name=".example05.RequestWithTimeoutActivity" />
        <activity android:name=".example04.MultipleNetworkRequestsActivity" />
        <activity android:name=".example03.ConcurrentApiCallsActivity" />
        <activity android:name=".example02.SequentialApiCallsActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".example01.SingleApiCallActivity" />
    </application>

</manifest>